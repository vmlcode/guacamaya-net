# Base URL the waitlist form POSTs to. Empty/unset uses a relative "/waitlist"
# (works via the Vite dev proxy locally, and works in production because the
# backend serves this build from its own origin at GET / — same-origin, no
# CORS needed).
VITE_WAITLIST_API_URL=
